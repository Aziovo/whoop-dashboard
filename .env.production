# Production environment variables
# These are set in Vercel dashboard - do NOT commit secrets here!
# Only set environment variables in Vercel (Settings → Environment Variables)

# For reference (set in Vercel, not here):
# VITE_WHOOP_CLIENT_ID=<from Vercel dashboard>
# VITE_WHOOP_CLIENT_SECRET=<from Vercel dashboard>
VITE_WHOOP_REDIRECT_URI=https://www.ekoonict.com/auth/callback
